## Example .env for VM_Manager
# Change these values as needed. Do NOT commit secrets to git.

SECRET_KEY={{ secret_key | default('dev-secret-change-me') }}
# LDAP example (optional)
# LDAP_HOST=ldap.example.local
# LDAP_BASE_DN=dc=example,dc=local
# LDAP_BIND_USER_DN=cn=binduser,dc=example,dc=local
# LDAP_BIND_USER_PASSWORD=changeme
